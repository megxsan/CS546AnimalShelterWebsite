<section>
	<h1>My Dogs</h1>
	{{#each dogs}}
		{{#if name}}
			<article class="dog">
				<h2><a class="myDog" href="/account/dogs/{{_id}}">{{name}}</a></h2>
			</article>
		{{/if}}
	{{/each}}
	<a href="/account/dogs/add">Create Dog Post</a>
</section>